using Client.ViewModels.Base;

namespace Client.ViewModels
{
    public class MapViewModel : BaseViewModel
    {
        public MapViewModel(IServiceProvider serviceProvider) : base(serviceProvider)
        {
        }
    }
}
